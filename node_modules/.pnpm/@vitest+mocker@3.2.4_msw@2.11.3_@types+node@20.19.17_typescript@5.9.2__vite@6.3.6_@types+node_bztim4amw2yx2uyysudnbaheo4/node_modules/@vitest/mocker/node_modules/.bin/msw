#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../msw@2.11.3_@types+node@20.19.17_typescript@5.9.2/node_modules/msw/cli/index.js" "$@"
else
  exec node  "$basedir/../../../../../../msw@2.11.3_@types+node@20.19.17_typescript@5.9.2/node_modules/msw/cli/index.js" "$@"
fi
