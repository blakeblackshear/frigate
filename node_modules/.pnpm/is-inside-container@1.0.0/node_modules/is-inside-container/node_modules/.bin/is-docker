#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../is-docker@3.0.0/node_modules/is-docker/cli.js" "$@"
else
  exec node  "$basedir/../../../../../is-docker@3.0.0/node_modules/is-docker/cli.js" "$@"
fi
