#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../svgo@3.3.2/node_modules/svgo/bin/svgo" "$@"
else
  exec node  "$basedir/../../../../../svgo@3.3.2/node_modules/svgo/bin/svgo" "$@"
fi
